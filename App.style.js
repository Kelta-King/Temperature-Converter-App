import { StyleSheet } from "react-native"

export const appStyle = StyleSheet.create({
    root: {
        flex: 1,
        justifyContent: 'center',
    },
    backgroundImage: {
        height: '100%',
    }
})
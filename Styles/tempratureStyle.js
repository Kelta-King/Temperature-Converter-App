import { StyleSheet } from "react-native"

export default tempratureStyle = StyleSheet.create({
    container: {

    },
    value: {
        fontSize: 70,
        fontWeight: 'bold',
        color: '#fff'
    }
})
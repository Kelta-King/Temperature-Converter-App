import { StyleSheet } from "react-native"

export default tempratureStyle = StyleSheet.create({
    container: {

    },
    value: {
        fontSize: 56,
        fontWeight: 'bold',
        color: '#fff'
    }
})
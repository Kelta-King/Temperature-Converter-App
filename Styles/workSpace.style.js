import { StyleSheet } from "react-native"

export const workSpaceStyle = StyleSheet.create({
    container: {
        height: 450,
        alignItems: "center",
        justifyContent: "space-evenly",
        padding: 16
    }
})